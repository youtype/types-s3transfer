packages:
  - name: s3transfer
    path: ./s3transfer-stubs
    checks:
      flake8: false
      pyright: false
      mypy: false
      stubtest: true
    snapshots:
      stubtest: |-
        error: s3transfer.compat.BaseManager.shutdown is not present at runtime
        Stub: at line 150 in file ./s3transfer-stubs/compat.pyi
        def (self: multiprocessing.managers.BaseManager)
        Runtime:
        MISSING

        error: s3transfer.processpool.BaseManager.shutdown is not present at runtime
        Stub: at line 150 in file ./s3transfer-stubs/processpool.pyi
        def (self: multiprocessing.managers.BaseManager)
        Runtime:
        MISSING
